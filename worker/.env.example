# Shared secret between the Worker and Hare. Generate a long random string;
# anyone holding it can read and write the encrypted snapshots.
#
# The snapshots themselves stay unreadable without the master password, so this
# token guards storage access, not the contents.
SYNC_TOKEN=
